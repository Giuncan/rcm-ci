name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:   
    - name: Generate Warning Annotations
      run: |
        text="This is a Warning, or isn't it? https://nightly.link/RemoteConnectionManager/RCM/actions/runs/${{ github.run_id }}"
        echo "::warning file=app.js,line=1,col=5::$text"
    
    - name: Build the Docker image
      run: touch x.aaa

    - uses: actions/upload-artifact@v4
      with:
        path: x.aaa

    
